# Milkdown 迁移收官计划 v2（2026-03-23）

## 背景

Step 3 已完成“首轮结果回填文档”，但因当前执行环境无真机能力，尚无法完成 Android/WebView 版本矩阵实测。  
因此迁移仍处于“功能迁移完成、验收闭环未闭合”的状态，需要 v2 计划推进收官。

## 目标

1. 在真实设备矩阵上完成回归清单执行。
2. 采集并回填 Step 2 定义的性能基线指标。
3. 形成可复用的迁移总结 v2（仅在验收闭环通过后产出）。

## 分步计划

### Step 1（本次完成）— 固化真机执行安排与证据模板

- 明确最小设备矩阵（至少覆盖 Android 12/13/14 各 1 台）；
- 统一记录字段（设备型号、系统版本、WebView 版本、执行人、日期、分支）；
- 固定证据格式（清单勾选 + 性能表 + 问题单链接）。

### Step 2 — 执行真机回归与性能采样

- 按 `docs/milkdown-device-regression-checklist.md` 完整执行；
- 按 `docs/milkdown-performance-baseline.md` 采样并记录；
- 输出通过/风险/阻断结论。

### Step 3 — 收官判定与总结 v2

- 若通过：更新迁移状态为“完整收官”，产出迁移总结 v2；
- 若未通过：沉淀未通过项与修复清单，进入下一轮计划。

## 本次落地项

- ✅ 已完成 Step 1（执行安排与证据模板落地）。
- ⏭ Step 2/3 待真实设备执行。
